---
# movies dummy service load balancer
kind: Service
apiVersion: v1
metadata:
  name: movies-service
spec:
  #type: NodePort
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 7000
      protocol: TCP
      #nodePort: 31000
  selector:
    app: movies-service